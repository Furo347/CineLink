services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    container_name: cinelink-uptime-kuma
    restart: always
    ports:
      - "3002:3001"
    volumes:
      - uptime-kuma-data:/app/data

volumes:
  uptime-kuma-data: